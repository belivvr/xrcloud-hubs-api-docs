---
sidebar_position: 6
title: 3D Models
---

import { Bold, APIUrl } from '@site/src/components/text';

> Hubs API URL: <a><APIUrl /></a>

## Get many 3D Models

> **[GET]** `/v1/3d-models?take=`<Bold>$\{take\}</Bold>`&page=`<Bold>$\{page\}</Bold>

:::info Request

#### Headers
- Authorization: Bearer {accessToken}

#### URL Query Parameters
- `take`: number of rooms to return
- `page`: page number to return
- `custom-data`: custom data to filter
- `custom-data-disabled`: disable `custom-data` and return all data

:::

:::info Response

- Status: `200 OK`
- Content-Type: `application/json`

```json
{
  "count": 10,
  "data": [
    {
      "id": "3d-model uuid",
      "name": "3d-model name",
      "thumbnailUrl": "https://...",
      "glbUrl": "https://...",
      "active": false, // boolean
      "accessKey": "project uuid",
      "created": "created date",
      "updated": "latest update date"
    },
    ...
  ]
}
```

:::

## Get a 3D Model information

> **[GET]** `/v1/3d-models/`<Bold>$\{id\}</Bold>

:::info Request

#### Headers
- Authorization: Bearer {accessToken}

:::

:::info Response

- Status: `200 OK`
- Content-Type: `application/json`

```json
{
  "id": "3d-model uuid",
  "name": "3d-model name",
  "thumbnailUrl": "https://...",
  "glbUrl": "https://...",
  "active": false, // boolean
  "accessKey": "project uuid",
  "created": "created date",
  "updated": "latest update date"
}
```

:::

## Create a 3D Model

> **[POST]** `/v1/3d-models`

:::info Request

#### Headers
- Authorization: Bearer {accessToken}
- Content-Type: `multipart/form-data; boundary=----WebKitFormBoundary...`

#### Body
- glb: base64 Buffer (example: data:application/octet-stream;base64,...)
- thumbnail: base64 Buffer (example: data:application/octet-stream;base64,...)
- name: string
- active: boolean

:::

:::info Response

- Status: `201 Created`
- Content-Type: `application/json`

```json
{
  "id": "3d-model uuid",
  "name": "3d-model name",
  "thumbnailUrl": "https://...",
  "glbUrl": "https://...",
  "active": false, // boolean
  "accessKey": "project uuid",
  "created": "created date",
  "updated": "latest update date"
}
```

:::


## Update a 3D Model

> **[PUT/PATCH]** `/v1/3d-models/`<Bold>$\{id\}</Bold>

:::info Request

#### Headers
- Authorization: Bearer {accessToken}
- Content-Type: `multipart/form-data; boundary=----WebKitFormBoundary...`

#### Body
- glb(optional): base64 Buffer (example: data:application/octet-stream;base64,...)
- thumbnail(optional): base64 Buffer (example: data:application/octet-stream;base64,...)
- name(optional): string
- active(optional): boolean

:::

:::info Response

- Status: `204 No Content`

:::

## Delete a 3D Model

> **[DELETE]** `/v1/3d-models/`<Bold>$\{id\}</Bold>

:::info Request

#### Headers
- Authorization: Bearer {accessToken}

:::

:::info Response

- Status: `204 No Content`

:::
